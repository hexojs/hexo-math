// Generated by CoffeeScript 1.6.3
var Command, Log, colors, log;

colors = require('colors');

Log = require('./Log');

log = new Log();

module.exports = Command = (function() {
  function Command(callback) {
    this.callback = callback;
  }

  Command.prototype.execute = function(opt) {
    try {
      return this[opt]();
    } catch (_error) {
      log.error("Unknown command: " + opt);
      return hexo.call('help', {
        _: ['math']
      }, this.callback);
    }
  };

  Command.prototype.install = function() {
    return log.info("Run install");
  };

  Command.prototype.remove = function() {
    return log.info("Run remove");
  };

  return Command;

})();
