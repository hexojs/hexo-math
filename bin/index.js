// Generated by CoffeeScript 1.6.3
var Command, async, colors, extend, file, fs, mathOptions, packageInfo, util;

extend = hexo.extend;

util = hexo.util;

file = hexo.file;

async = require('async');

colors = require('colors');

fs = require('fs');

packageInfo = require('../package.json');

Command = require('./Command');

mathOptions = {
  desc: packageInfo.description,
  usage: '<argument>',
  "arguments": [
    {
      name: 'install',
      desc: 'Install MathJax dependencies.'
    }, {
      name: 'remove',
      desc: 'Removes MathJax dependencies.'
    }
  ]
};

hexo.extend.console.register("math", packageInfo.description, mathOptions, function(args, callback) {
  var cmd;
  cmd = new Command(callback);
  return cmd.execute(args._[0]);
});
